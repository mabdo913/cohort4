const oopCity = {};

class City {
    constructor(name, lat, lon, pop) {
        this.name = name,
        this.lat = lat,
        this.lon = lon,
         this.pop = pop;
    }

    show() {
        
    }
}

class Community {
    
}

export default { oopCity, City, Community};